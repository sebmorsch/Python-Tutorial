\textbf{Kontrollfragen}

\kontrollfrage{
	\item[\kontroll] Wofür werden Datenbanken verwendet?
	\item[\kontroll] Wofür wird SQLite am häufigsten verwendet?
	\item[\kontroll] Wie wird in SQLite eine neue Datenbank angelegt?
	\item[\kontroll] Welcher Befehl muss ausgeführt werden um einen bestehenden Eintrag zu ändern?
	\item[\kontroll] Was ist der Unterschied zwischen SQL-Datenbanken und NoSQL-Datenbanken?
	\item[\kontroll] Worauf muss beim Einrichten von MongoDB geachtet werden?
	\item[\kontroll] Wofür wird bei MongoDB die Methode find() verwendet?
	\item[\kontroll] Wofür wird die Methode count\_documents({}) verwendet?
}